name: Nightly Build and Test

on:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches:
      - main

permissions:
  packages: write
  contents: write

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Required to fetch all tags for git describe

      - name: Semantic Release
        id: version
        uses: cycjimmy/semantic-release-action@v4
        with:
          extra_plugins: |
            @semantic-release/commit-analyzer
            @semantic-release/release-notes-generator
            @semantic-release/github
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tag
        id: tag
        run: |
          if [ "${{ steps.version.outputs.new_release_published }}" = "true" ]; then
            echo "version=${{ steps.version.outputs.new_release_version }}" >> $GITHUB_OUTPUT
          else
            echo "version=nightly.$(date +'%Y%m%d')" >> $GITHUB_OUTPUT
          fi
      

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Tests
        id: run-tests
        env:
          # Required environment variables for app/tests
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          JWT_ALGORITHM: ${{ secrets.JWT_ALGORITHM }}
          MAIL_USERNAME: ${{ secrets.MAIL_USERNAME }}
          MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
          MAIL_SERVER: ${{ secrets.MAIL_SERVER }}
          MAIL_PORT: ${{ secrets.MAIL_PORT }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_FROM_NAME: ${{ secrets.MAIL_FROM_NAME }}
          DOMAIN: ${{ secrets.DOMAIN }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
        run: pytest src/tests 2>&1 | tee /tmp/pytest_output.txt; exit ${PIPESTATUS[0]}

      - name: Notify on Test Failure
        if: steps.run-tests.outcome == 'failure'
        env:
          MAIL_SERVER: ${{ secrets.MAIL_SERVER }}
          MAIL_PORT: ${{ secrets.MAIL_PORT }}
          MAIL_USERNAME: ${{ secrets.MAIL_USERNAME }}
          MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
        run: |
          AUTHOR_EMAIL=$(git log -1 --pretty=format:'%ae')
          FAIL_OUTPUT=$(cat /tmp/pytest_output.txt)
          echo "Sending failure email to $AUTHOR_EMAIL"
          python scripts/send_email.py \
            --to "$AUTHOR_EMAIL" \
            --subject "Nightly Build Failed: Test Failure" \
            --body "The nightly build failed due to test errors.\n\nTest Output:\n$FAIL_OUTPUT"

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/fastapi-beyond-crud:${{ steps.tag.outputs.version }}
            ghcr.io/${{ github.repository_owner }}/fastapi-beyond-crud:nightly
